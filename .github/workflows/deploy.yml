name: Deploy Customs Bot
on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
      
      - name: 📋 Show info
        run: |
          echo "🚀 Customs Bot Deployment Test"
          echo "If you see this, GitHub Actions works!"
          echo "Repository: ${{ github.repository }}"
